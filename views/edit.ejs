<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-between mb-10">
            <a class="bg-blue-500 rounded-md px-3 py-2 text-sm inline-block" href="/profile">Back</a>
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5">Edit your post</h5>
        <form action="/update/<%= post._id %>" method="POST">
            <textarea placeholder="What's on your mind ??" class="p-3 outline-none w-1/3 block resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%= post.content %></textarea>
            <input class="px-3 py-2 w-40 bg-yellow-500 text-black text-sm block rounded-md mt-2" type="submit" value="Update post">
        </form>
    </div>
</body>
</html>